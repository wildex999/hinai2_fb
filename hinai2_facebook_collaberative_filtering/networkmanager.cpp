#include "networkmanager.h"

#include <QString>

//The network manager has the task of connecting to Facebook Graph API and get data for the parser

NetworkManager::NetworkManager()
{
    //Add a request job
    void addGetRequestJob(QString query);
}
